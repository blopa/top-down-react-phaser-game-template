export const selectFonts = (state) => state.loadedAssets.fonts;

export const selectAtlases = (state) => state.loadedAssets.atlases;

export const selectImages = (state) => state.loadedAssets.images;

export const selectMaps = (state) => state.loadedAssets.maps;

export const selectTilesets = (state) => state.loadedAssets.tilesets;

export const selectJSONs = (state) => state.loadedAssets.jsons;
