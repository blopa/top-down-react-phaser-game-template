export const selectGameWidth = (state) => state.gameSettings.width;

export const selectGameHeight = (state) => state.gameSettings.height;

export const selectGameZoom = (state) => state.gameSettings.zoom;
