export const selectFonts = (state) => state.assets.fonts;

export const selectAtlases = (state) => state.assets.atlases;

export const selectImages = (state) => state.assets.images;

export const selectMaps = (state) => state.assets.maps;

export const selectTilesets = (state) => state.assets.tilesets;

export const selectJSONs = (state) => state.assets.jsons;
